cat /etc/hosts

Contains a list of local DNS entries. This is a plain text file will be used by the system for looking up IP addresses BEFORE any DNS is used. Thus it is useful for things such as artificially changing your website address for testing purposes or blocking access to specific sites.